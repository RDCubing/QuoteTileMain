<Page
    x:Name="pageRoot"
    x:Class="QuoteTile.Favorites"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuoteTile"
    xmlns:common="using:QuoteTile.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <x:String x:Key="AppName">Favorites</x:String>
    </Page.Resources>

    <Grid x:Name="MainGrid">
        <Grid.Background>
            <ImageBrush Stretch="UniformToFill"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Background="#FF0d47a1" Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="backButton" Margin="39,59,39,0"
                    Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                    Style="{StaticResource NavigationBackButtonNormalStyle}"
                    VerticalAlignment="Top"
                    AutomationProperties.Name="Back"
                    AutomationProperties.AutomationId="BackButton"
                    AutomationProperties.ItemType="Navigation Button"/>

            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}"
                       Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1"
                       IsHitTestVisible="false" TextWrapping="NoWrap"
                       VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <!-- Description -->
                <ColumnDefinition Width="*"/>
                <!-- ListView -->
            </Grid.ColumnDefinitions>
            <!-- Dim overlay covering the whole background -->
            <Rectangle
        Fill="#88000000"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        Grid.ColumnSpan="2"
        IsHitTestVisible="False"/>

            <Border Grid.Column="0"
            Padding="25"
            Margin="0,0,0,0"
            Background="#88000000">

                <StackPanel>

                    <TextBlock Text="Favorites"
                       FontSize="26"
                       FontWeight="SemiBold"
                       TextWrapping="Wrap"/>

                    <TextBlock
                Text="Welcome to your Favorites! Here, you can easily access all the quotes you've saved for quick reference. Browse, scroll, and revisit your favorite inspirations anytime, all in one convenient place."
                FontSize="15"
                TextWrapping="Wrap"
                Margin="0,8,0,0"/>

                </StackPanel>

            </Border>

            <!-- Favorites ListView (Right Side) -->
            <ListView x:Name="FavoritesListView"
                      Grid.Column="1"
                      IsItemClickEnabled="True"
                      ItemClick="FavoritesList_ItemClick"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"
                      ScrollViewer.HorizontalScrollMode="Disabled"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollMode="Enabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#AA121216"
                                CornerRadius="12"
                                Margin="10"
                                Padding="15"
                                BorderBrush="#22FFFFFF"
                                BorderThickness="1">
                            <StackPanel>
                                <TextBlock Text="{Binding Content}"
                                           TextWrapping="Wrap"
                                           Foreground="White"
                                           FontSize="18"/>
                                <TextBlock Text="{Binding Author}"
                                           Foreground="LightGray"
                                           Margin="0,12,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>